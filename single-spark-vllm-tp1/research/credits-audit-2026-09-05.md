# Credits audit: tonyd2wild/Qwen3.8-Flash-Next-NVFP4-DGX-Spark (main @ 65b5788, 2026-09-05)

Read-only audit of the clone at `/Users/clawdbot/.openclaw/workspace/qwen38fn-dgx-spark-repo`. Evidence was gathered by fetching the vLLM nightly files at `8a728663c1c3eeace834a95f5654fa653cc1998c` and the PR diffs from GitHub, applying them in a scratch dir and byte-comparing against the repo; PR/issue authors, titles, states and dates come from the GitHub API; community READMEs (MiaAI-Lab single and dual, blazux, lancelind, getrefined, sfxnz) were read, none of their code was copied anywhere. All times UTC unless marked ET.

## Findings table

| # | Item | File:line | Status | Evidence / suggested fix |
|---|---|---|---|---|
| 1 | `ops_ple.py` = nightly + PR #55375 | `single-spark-vllm-tp1/patch/upstream-overlays/ops_ple.py` | OK | `git apply` of `pull/55375.diff` onto the nightly `vllm/models/qwen4_exp/nvidia/ops/ple.py` is clean; `cmp` with the repo copy: byte-identical. PR #55375: author peakcrosser7, title "[Bugfix][Qwen4Exp] fix state index strides in fused PLE conv", merged 2026-09-05T14:02:20Z. README (root:88, lane:35) and launcher:24 wording is correct. |
| 2 | `ops_qsa.py`, `qsa.py`, `platforms_interface.py` = nightly + PR #54846 | `patch/upstream-overlays/{ops_qsa.py,qsa.py,platforms_interface.py}` | OK | The raw PR diff does NOT apply directly to the nightly (context drift in `ops/qsa.py`: 406 lines changed between the PR merge-base `80389cfe` and the nightly), so the overlay was rebased by hand. A 3-way merge (nightly, merge-base `80389cfe`, PR head `1e11f40bbc`) is conflict-free and all three results are byte-identical to the repo copies; `qsa.py` is verbatim the PR-head file. Every `+` line of the PR is present in the overlay and no overlay `+` line is outside the PR (233/233, 232/232, 20/20). PR #54846: author andreasgru, title "[Model] Qwen4Exp: fp8_e4m3 and nvfp4 KV cache on the QSA path", state open, created 2026-09-01, commits 82fb6fad50 and 1e11f40bbc (both 2026-09-01). "Unmodified" claim (root README:88, lane README:32-36, launcher:23-25) is accurate. |
| 3 | `modelopt.py` overlay contains only our two fixes | `patch/upstream-overlays/modelopt.py`, `patch/modelopt_mtp_index.diff` | OK | `diff -u nightly overlay` has 4 hunks, +47/-0 lines, and its body is identical to the shipped `modelopt_mtp_index.diff`: `import re`; new `_quantized_layer_group_size()`; draft-local candidates in `_quantized_layer_prefix_candidates()`; `FP8_BLOCK_SCALES`/`FP8_BLOCK` branch returning `Fp8MoEMethod(Fp8Config(is_checkpoint_fp8_serialized=True, activation_scheme="dynamic", weight_block_size=[gs, gs]))`. Nothing else. Upstream SPDX header kept. No text lifted from any open PR (see 4). |
| 4 | Prior art on the two modelopt fixes | root README:87, lane README:40-43, `research/upstream-issue-modelopt-mtp-index.md:16`, launcher:26 | FIX (accuracy) | Nobody has filed a vLLM PR/issue for this yet (searches "FP8_BLOCK_SCALES modelopt mixed", "w2_weight_scale_inv Qwen3.8", "hf_quant_config mtp": 0 hits; "mtp.layers modelopt" hits are other models: #53790 juhi10071998 NemotronHMTP 08-25, #46316/#38832 mtp.fc). Related but different: PR #50617 (cboddeti-nv, open since 07-31, adds `FP8_PB`/`FP8_PB_WO` Linear dispatch to modelopt_mixed, no RoutedExperts branch) and PR #54367 (jschmied, merged 08-29, `FP8_PER_CHANNEL_PER_TOKEN` dispatch in mixed). BUT two independent equivalents of both fixes were published the same day: (a) sfxnz/Qwen3.8-Flash-Next-NVFP4-vLLM-2x-DGX-Spark PR #2, single commit 1bf8badf60 authored 2026-09-05T13:51:34Z, merged 16:43Z, MIT; `docker/modelopt.py` remaps `mtp.layers.<n>` to `mtp.layers.0` (fixed marker, not candidates) and routes `FP8_BLOCK_SCALES` to `Fp8MoEMethod`; their README says "MTP load: this lab". (b) MiaAI-Lab/Qwen3.8-Flash-Next-Dual-DGX-Sparks commit ee33141b 2026-09-05T16:06:59Z, AGPL-3.0; `files/patch_modelopt_fp8_block_moe.py` + `files/overlay/mtp.diff`, CHANGELOG "nvidia NVFP4 checkpoint support (2026-09-05)". Ours: index fix written between 11:54 and 11:59 AM ET (15:54-15:59Z, memory notes), block-scales fix by 16:11Z (file mtime in the diff header), commit 0d507df 16:42:44Z. Textual overlap with sfxnz: 3 lines, all `Fp8Config` kwargs dictated by vLLM's API; different mechanism, different comments. PixelML PR #6 (seanphan, 12:52Z) hit the same error with no fix. Verdict: ours is independent and correctly labelled "ours", but sfxnz was about two hours earlier, so nothing should read as "first" and the upstream report should cite both as corroboration. Wording in edits A, E, J, Q. |
| 5 | `ple_mmap.py` / `ple_layer` hooks vs PR #54129 (Trosfy) | `patch/ple_mmap.py`, `patch/ple_layer.diff` | OK (independent) | PR #54129: author Trosfy, "[Model] Support disk-backed (mmap) PLE table for Qwen3.8-Flash-Next (VLLM_PLE_MMAP)", open, created 08-28. Designs differ: PR = `np.memmap` views, `posix_fadvise(WILLNEED)` readahead, gather moved into Model Runner V2 input prep (`MmapNgramEmbedding.gather_into`, `prepare_mmap_rows`), `VLLM_PLE_MMAP_*` envs registered in `envs.py`, header-read of `weight_scale`; ours = `preadv` thread pool, `POSIX_FADV_RANDOM`, pinned staging, gather inside the layer forward through a custom op registered as a splitting op, `QWEN4EXP_PLE_MMAP*` envs. Identical non-trivial lines in `ple_mmap.py`: 7, all boilerplate (`# SPDX-License-Identifier: Apache-2.0`, `from __future__ import annotations`, `from concurrent.futures import ThreadPoolExecutor`, `with open(path, "rb") as f:`, `for name, meta in header.items():`, `fd = os.open(path, os.O_RDONLY)`, `self._fds: dict[str, int] = {}`). In the layer hooks: 1 shared line, `loaded.add("ngram_embedding.weight")`, which is nightly context (`ple_layer.py:522`). Class names `PLEMmapTable` vs `MmapPleTable` are convergent. No copied comments. |
| 6 | `ple_mmap.py` vs community single-Spark repos | lane README:27, root README:88, `patch/compilation.py:772` | FIX (name convergent prior art) | blazux/qwen3.8-Flash-DGX (Apache-2.0, repo created 08-27) README lines 360 and 392-393: "mmap PLE table (opaque splitting op)" and "Full torch.compile is off (an Inductor int64-indexing assert on sm_121); the serve script uses PIECEWISE CUDA graphs with the PLE lookup as a splitting op". That is the same graph mode this lane ships (custom op in `_attention_ops`, `cudagraph_mode PIECEWISE`), and Trosfy's early #54129 revision also went through a custom-op body. lancelind/qwen3.8-Flash-DGX is a GitHub fork of blazux (created 08-29, parent blazux), not a separate source; MiaAI credits it for the FP8-KV approach. MiaAI-Lab single-Spark (AGPL-3.0): packed 27 GB table, `MADV_RANDOM`, `HOST_RESERVE_GIB`, host-side handshake; none of it is in our patch. The research note's own rule (gb10-prior-art:3) says adopted ideas must be named in the README; the piecewise/splitting-op mode is at minimum convergent with blazux and should be named. Edits C, D. |
| 7 | SPDX headers on modified vLLM files | `upstream-overlays/*.py:1-2`, `patch/ple_layer.py:1-2`, `patch/compilation.py:1-2` | OK | All seven carry both upstream lines (`SPDX-License-Identifier: Apache-2.0`, `SPDX-FileCopyrightText: Copyright contributors to the vLLM project`), same as the nightly. `ple_layer.py`, `compilation.py`, `modelopt.py` carry inline "Kai/2Wild" change notices (Apache-2.0 section 4(b)). |
| 8 | License line on our new file | `patch/ple_mmap.py:1-4` | FIX (minor) | Has `# SPDX-License-Identifier: Apache-2.0` and "Kai / 2Wild, 2026-09-05 ... Own implementation" but no copyright holder. Add `# SPDX-FileCopyrightText: 2026 Tony DeAngelo (Tech2Wild / 2Wild)` as line 2. Also line 59 is a leftover from the mmap version: `self._files: dict[str, tuple[mmap.mmap, int]] = {}` with no `import mmap` (inert under `from __future__ import annotations`, but misleading). |
| 9 | Repository license | repo root | FIX | No `LICENSE`, `NOTICE` or `COPYING` anywhere (`find` on the tree). The repo redistributes modified Apache-2.0 vLLM files and the Apache-2.0 sglang PR diff, and states Apache-2.0 in its own header, but recipients get no copy of the license (Apache-2.0 section 4(a)). Add `LICENSE` (Apache-2.0, Copyright 2026 Tony DeAngelo) and a `NOTICE` listing third-party content. Edit L. |
| 10 | Change notice on the two PR overlays | `upstream-overlays/ops_qsa.py`, `qsa.py`, `platforms_interface.py`, `ops_ple.py` | FIX (minor) | These differ from the nightly (PR applied) but carry no notice saying so; editing them would break the byte-identity claim, so add `upstream-overlays/PROVENANCE.md` with the sha256 manifest below. Edit M. |
| 11 | AGPL content in the repo | `lanes/sglang-tp2/community-fix/sm121_varlen.py`, `community-fix/README.md:12-13` | FIX (licensing) | The file is, per `community-fix/README.md:12`, "extracted from MiaAI-Lab/Qwen3.8-Flash-Next-Dual-DGX-Sparks (start.sh)". That repo is AGPL-3.0 (GitHub license field; README: "AGPL-3.0-or-later ... every source file carries an SPDX-License-Identifier: AGPL-3.0-or-later header"). Only 39 of the file's 119 non-trivial lines are verbatim from sglang PR #36845 (Apache-2.0, author BBuf); the function names `_qsa_one_query_varlen_kernel` / `qsa_sm121_varlen_attention` and the "DSpark extra vs upstream: K/V may be fp8" extension are not in the upstream PR, so it is a MiaAI-authored derivative. Our copy has no license header, MiaAI is not named as copyright holder, the repo has no LICENSE, and lane README:22 still says "no borrowed code". The vLLM lane and research notes are clean (gb10-prior-art:42 explicitly says MiaAI code is not to be copied, and none is). Recommended: delete `community-fix/sm121_varlen.py` (the Apache original is already bundled as `lanes/sglang-tp2/pr36845.diff`, byte-identical to the upstream PR diff body) and link to MiaAI's repo instead. If it stays, it needs an AGPL-3.0-or-later header, a MiaAI copyright line, an AGPL LICENSE in that directory and a root README note. Edit N. |
| 12 | `patch/ple_layer.diff` is stale | `patch/ple_layer.diff` (whole file), lane README:24 | FIX | `diff -u nightly patch/ple_layer.py` has 7 hunks; the shipped diff has 6 with different offsets (`@@ -459,8 +476,49 @@` vs `+476,53`). The shipped diff still has `self._ple_gpu_buf = None` and builds the table inside `_ple_mmap_lookup`; the `.py` (and lane README:28) uses `_ple_row_bytes`/`_ple_dtype` and builds the reader eagerly at the end of `load_weights`. `compilation.diff` and `modelopt_mtp_index.diff` match their files exactly. Regenerate. Edit I. |
| 13 | README attribution accuracy (names, numbers, states) | root README, lane README, `research/*.md` | OK with 2 softenings | Verified against the API: peakcrosser7 #53896 merged 2026-08-31, #53899 open, #54371 open, #55375 merged 09-05; andreasgru #54846 open; Nanetnounou #54426 is an issue (RFC), body says "the four non-obvious integration points" (lane README:36 correct); Trosfy #54129 open; jschmied #54125 issue (body: workaround `VLLM_USE_DEEP_GEMM=0`), #55122 open, #54948 open, #55430 open (created 09-05), #55467 closed unmerged (parallel to #55375); brainatworkharris #54173 issue (body: only `--no-enable-prefix-caching` never faults); sychen52 #54882 merged 09-03; stecasta #55334 open; jagat-primitive-org #54070 open; gau-nernst #55272 open and merged QSA PRs #54513/#54873/#54915; tsw2k #54629; #54122 (jhonypyter5, unnamed). blazux's README confirms the research note's Saren-Arterius (FLA 99 KiB gate, gather fast path, fp8_convert), jschmied (deterministic top-k, M%4 pad, methodology) and Nanetnounou (fp8 KV patch) items. Flaviu Vlaicu confirmed: vlaicu.io/posts/dgx-vllm (2026-06-20) recommends `CUTE_DSL_ARCH=sm_121a`, `VLLM_USE_DEEP_GEMM=0`, `FLASHINFER_DISABLE_VERSION_CHECK=1`. eugr confirmed (eugr/spark-vllm image, DEPLOY-REPORT:62). Soften: gb10-prior-art:42 "Daniel Han (`CUTE_DSL_ARCH=sm_121a` origin)": his X post and the unsloth model-card "DGX Spark Instructions" popularized `export CUTE_DSL_ARCH=sm_121a` for Spark, but the variable predates that (NVIDIA/cutlass issues #2800/#2802, Nov 2025; vLLM PR #40082, Apr 2026); say "popularized for DGX Spark by". gb10-prior-art:20 "PR #48375 variant": #48375 is potto007 "[BugFix] Honor drop_eagle_block in MambaManager" (open since 07-12); the relation to the GDN prefix-cache fix was not verifiable here. Not verifiable through the API (forum/X/PR-comment sources): Conselara, hmlee0315 "first #54129 report", dolf3131, provsalt / raymond.goo, changtimwu, Starkweather Digital, hasso5703, azampatti, styles01, kubesimplify, tsw2000. These are research-note only. |
| 14 | Missing credits for ideas in the SHIPPED stack | lane README:54, :72, launcher:4, :51-53, :60, root README:88 | FIX | (a) `VLLM_USE_DEEP_GEMM=0` (launcher:53; README:54 says only "sm_121 findings"): name vLLM issue #54125, jschmied. (b) "danger line around 10 GB available" (README:72): MiaAI-Lab single README ("Keep host MemAvailable at or above ~10 GiB under load") and blazux README (gmu 0.85 drifted into swap after a day, 0.875 OOM-killed on a 300k prefill with MTP). (c) piecewise + splitting op: blazux / Trosfy (item 6). (d) #54173 is cited by number only (README:54, launcher:4): add brainatworkharris. (e) `--no-enable-flashinfer-autotune` (launcher:60): research note attributes the autotune-cache failure to jschmied; one clause in README:54. (f) `VLLM_USE_V2_MODEL_RUNNER=1` (launcher:53): the nightly already auto-selects V2 (`envs.py:301` default None, `vllm/config/vllm.py:652-684`), so it is a pin, not a borrowed trick; optional mention. (g) `CUTE_DSL_ARCH=sm_121a`, `TORCH_CUDA_ARCH_LIST=12.1a`, `expandable_segments`, `drop_caches`: already in Tony's own recipes since May-Aug 2026 (memory: m27 05-10, m3 06-12, DS4 07-03, sglang lane 08-26), no new credit required. Research-note-only ideas that are NOT in the shipped stack and are fine as is: deterministic top-k #55122, MoE finalize #54948, tile-union #55430, Saren-Arterius FLA gate and prefix-cache fix, MiaAI stream-mem-ops / HOST_RESERVE / packed table, lancelind. FP8-KV RFC #54426 is credited in the lane README but not the root README (optional). Edits C, F, G, J. |
| 15 | Launcher comment describes only one of the two modelopt fixes | `launch/qwen38fn-nvidia-tp1.sh:26` | FIX (minor) | Line 26 mentions the index remap only; the FP8_BLOCK_SCALES branch is missing. Edit J. |
| 16 | "Two-file patch" / "ours" wording | lane README:3, root README:86 | FIX (minor) | Patch is three files (`ple_mmap.py`, hooks in `ple_layer.py`, one line in `compilation.py`) plus the modelopt overlay; `ple_layer.py` is the nightly file plus our hooks, not "ours" outright. Edits B, H. |
| 17 | sglang lane move: files and credits | `lanes/sglang-tp2/*` | OK | Commit 0d507df renamed all 12 files R100 (unchanged content) and copied README.md (C100). Credits intact: `community-fix/README.md:12-13` (MiaAI + sglang #36806/#36845), `README.md:85-89, :102, :192` (MiaAI-Lab, SGLang maintainers), `KNOWN-ISSUES:15` and `DEPLOY-REPORT:363` (#36537), `Dockerfile...triton-varlen:4-14, :33` (#36806/#36845). `pr36845.diff` body is identical to the upstream PR diff. |
| 18 | sglang lane move: relative links | `lanes/sglang-tp2/*.md`, root README:7 | OK | Every intra-lane link resolves (README -> pr36845.diff, KNOWN-ISSUES, community-fix/, launcher, models.yml, DEPLOY-REPORT, MROPE-ANALYSIS, INCIDENT, load_test_qwen.py; DEPLOY-REPORT -> KNOWN-ISSUES; community-fix/README -> sm121_varlen.py). Root README:7 -> `lanes/sglang-tp2/` resolves. External repos (Fleet-Deploy README:58, getrefined README:6) link to the repo root only, so nothing external broke. Pre-existing dangling references not caused by the move: `INCIDENT...:5` `QWEN38FN-SPARK-LANE-HANDOFF.md` (never in repo history), `~/...` paths, sglang source filenames, `community-fix/README.md:12` `start.sh` (MiaAI's file). |
| 19 | sglang lane: stale states and unnamed author | `lanes/sglang-tp2/README.md:89, :102, :201`, `Dockerfile...triton-varlen:11` | FIX (minor) | sglang #36806 and #36845 are both by BBuf (never named); #36845 merged 2026-08-30T16:05Z, so README:201 "Upstream watch: #36845 merging" and Dockerfile:11 "#36845, open" are stale; #36497 (JustinTong0323) is still open (README:201 correct). Edit O. |
| 20 | Research note stale line | `research/gb10-prior-art-and-pitfalls-2026-09-05.md:9` | FIX (minor) | Says "Landed AFTER our nightly, we do not have it: PR #55375 ... Mitigation until rebase: MTP off"; the overlay now applies it. Edit P. |
| 21 | Untracked launcher | `single-spark-vllm-tp1/launch/qwen38fn-nvidia-tp2.sh` | note | `git status` shows it untracked; it is not in the pushed repo. Commit or delete. |

## Concrete edits

A. `README.md:87` replace the bullet with: "`modelopt.py` overlay: ours, written 2026-09-05. Two fixes so NVIDIA's MTP head loads (draft-local layer index in the quant config; a branch for 128x128 block-scaled FP8 experts). The same two gaps were fixed independently the same day by sfxnz (`sfxnz/Qwen3.8-Flash-Next-NVFP4-vLLM-2x-DGX-Spark`, MIT, commit 13:51 UTC) and by MiaAI-Lab (`Qwen3.8-Flash-Next-Dual-DGX-Sparks`, AGPL-3.0, commit 16:07 UTC); no code is shared between the three. Upstream issue draft in `single-spark-vllm-tp1/research/`."

B. `README.md:86` replace "`ple_mmap.py` and `ple_layer.py`: ours." with "`ple_mmap.py` (new file) and the hooks in `ple_layer.py` (nightly file plus our hooks, `patch/ple_layer.diff`): ours."

C. `README.md:88` append after "the implementation here is independent.": "The piecewise-CUDA-graph mode with the PLE lookup registered as a splitting op is the same mode blazux's single-Spark recipe ships (`blazux/qwen3.8-Flash-DGX`, Apache-2.0); Trosfy's early #54129 revision also went through a custom op. Launcher settings taken from community findings: `VLLM_USE_DEEP_GEMM=0` (vLLM issue #54125, jschmied), prefix caching off (vLLM issue #54173, brainatworkharris). FP8 KV: PR #54846 (andreasgru) and RFC #54426 (Nanetnounou)."

D. `single-spark-vllm-tp1/README.md:27` append: "This is the same graph mode blazux's recipe uses (`vllm_ple_mmap.py` as an opaque splitting op under PIECEWISE); here it comes from vLLM's own `_attention_ops` list, which already carries `qwen4_exp_compute_ple_ngram_ids`."

E. `single-spark-vllm-tp1/README.md:43` replace "Both are being reported upstream (`research/upstream-issue-modelopt-mtp-index.md`)." with: "Both are being reported upstream (`research/upstream-issue-modelopt-mtp-index.md`). The same two gaps were fixed independently the same day by sfxnz (`docker/modelopt.py` in `sfxnz/Qwen3.8-Flash-Next-NVFP4-vLLM-2x-DGX-Spark`, MIT, commit 13:51 UTC) and by MiaAI-Lab (`files/patch_modelopt_fp8_block_moe.py` in `Qwen3.8-Flash-Next-Dual-DGX-Sparks`, AGPL-3.0, commit 16:07 UTC); the three implementations share no code."

F. `single-spark-vllm-tp1/README.md:54` replace the last two sentences with: "Prefix caching is off by default (`PREFIX_CACHE_ARG`) until the GDN prefix-cache crash reported in vLLM #54173 (brainatworkharris) is verified fixed in this image. The launcher sets `VLLM_USE_DEEP_GEMM=0` (DeepGEMM faults on sm_121: vLLM issue #54125, jschmied), `VLLM_USE_V2_MODEL_RUNNER=1` (pins the V2 runner the nightly already selects), and `--no-enable-flashinfer-autotune` (FlashInfer autotune-cache failures on GB10 reported by jschmied); `CUTE_DSL_ARCH=sm_121a`, `TORCH_CUDA_ARCH_LIST=12.1a` and `expandable_segments` are the standard Spark vLLM environment (see the Flaviu Vlaicu playbook and the research notes)."

G. `single-spark-vllm-tp1/README.md:72` replace "Community reports put the danger line around 10 GB available (swap creep, OOM kills on huge prefills)" with "Community reports put the danger line around 10 GB available: MiaAI-Lab's single-Spark README says keep MemAvailable at or above ~10 GiB under load, and blazux reports gmu 0.85 drifting into swap after a day and 0.875 OOM-killed on a 300K prefill with MTP".

H. `single-spark-vllm-tp1/README.md:3` replace "a two-file patch written for this lane" with "a three-file patch (`ple_mmap.py`, hooks in `ple_layer.py`, one line in `compilation.py`) plus a two-fix `modelopt.py` overlay, all written for this lane".

I. Regenerate `single-spark-vllm-tp1/patch/ple_layer.diff`: `diff -u <nightly 8a728663 vllm/models/qwen4_exp/nvidia/ple_layer.py> single-spark-vllm-tp1/patch/ple_layer.py` with `a/`/`b/` labels as the current header uses. Verify with `patch --dry-run`.

J. `single-spark-vllm-tp1/launch/qwen38fn-nvidia-tp1.sh:26` replace with: "#   modelopt.py: OUR two fixes (Kai, 2026-09-05): (1) draft-local MTP index candidates so mtp.layers.48 finds ModelOpt's mtp.layers.0 entry; (2) FP8_BLOCK_SCALES routed experts -> Fp8MoEMethod (128x128 block scales). Same gaps fixed independently the same day by sfxnz (MIT) and MiaAI-Lab (AGPL); no shared code." Line 4: "#54173" -> "#54173 (brainatworkharris)".

K. `single-spark-vllm-tp1/patch/ple_mmap.py`: insert line 2 `# SPDX-FileCopyrightText: 2026 Tony DeAngelo (Tech2Wild / 2Wild)`; optionally change line 59 to `self._files: dict[str, tuple[int, int]] = {}` or delete it (unused).

L. Add `LICENSE` (Apache License 2.0 text, "Copyright 2026 Tony DeAngelo") and `NOTICE` at the repo root: "This repository contains: (1) files derived from vLLM (Apache-2.0, Copyright contributors to the vLLM project): single-spark-vllm-tp1/patch/ple_layer.py, compilation.py, upstream-overlays/*.py, and the diffs beside them; (2) sgl-project/sglang PR #36845 by BBuf (Apache-2.0) as lanes/sglang-tp2/pr36845.diff; (3) see lanes/sglang-tp2/community-fix/README.md for the licensing of that directory."

M. Add `single-spark-vllm-tp1/patch/upstream-overlays/PROVENANCE.md`:
```
Base: vLLM main 8a728663c1c3eeace834a95f5654fa653cc1998c (image vllm/vllm-openai:nightly-8a728663...).
PR #55375 (peakcrosser7, merged 2026-09-05T14:02Z) applied to ops/ple.py.
PR #54846 (andreasgru, open; head 1e11f40bbc, merge-base 80389cfe) applied to ops/qsa.py, qsa.py, platforms/interface.py (3-way merge, no conflicts).
modelopt.py = nightly + ../modelopt_mtp_index.diff (ours).
sha256 nightly:
  cec929bed9c525ce1ecf362966af0da6e382645f5002d878a2f545f40b38e08e  vllm/models/qwen4_exp/nvidia/ops/ple.py
  ce71c93ce3f88be26b113c9b238bd179546ccbef4be8b01988a1cc5748eb0e73  vllm/models/qwen4_exp/nvidia/ops/qsa.py
  c501c2d2b647bdc93be3c0102f36a84446767b20cbff123314173e04757a026d  vllm/models/qwen4_exp/nvidia/qsa.py
  377522c3d6e9c71a69fe74db5137ccc1dbd254f4c486d4342544af06b998b45a  vllm/platforms/interface.py
  e1b745a13699c3bc75254f942dfc360276a6bc223ddbb304425aaf45f46a26c7  vllm/model_executor/layers/quantization/modelopt.py
sha256 PR diffs (fetched 2026-09-05):
  23001486299217b6d00ea00cb61fb20fb5c2c768a8fe46f4231e9652ba1c5219  pull/55375.diff
  336d618bb2bfd6e9db0372decc5421443242b6b8a208189d05dd41ad1c46eade  pull/54846.diff
sha256 overlays:
  cc8994b653dd4b59d6ba35b6ec10af34017e9944482ec67cad6a32a02c5dd00a  ops_ple.py
  1e928e2994233496e9ef447768e8b63acbedc22e242d4b2fc9f4ed861a1616c3  ops_qsa.py
  f2fc3fb43f6c6e84e892a995697fb59a85ee8df2ef2959943c96cf0233078da7  qsa.py
  443b556b984b306c0274c58af8658ef853c664b8beae6deefda41908f789d26d  platforms_interface.py
  eff707e7f42b12a0483f41461aaaf786399e860077b767a76dc236a869c7e303  modelopt.py
```

N. `lanes/sglang-tp2/community-fix/`: preferred, delete `sm121_varlen.py` and rewrite `README.md` line 3 and 12-13 as: "The reference kernel lives in MiaAI-Lab/Qwen3.8-Flash-Next-Dual-DGX-Sparks (AGPL-3.0-or-later; not vendored here) and ports sgl-project/sglang PRs #36806 and #36845, both by BBuf (Apache-2.0, merged 2026-08-28 and 2026-08-30). The upstream Apache-2.0 kernel is bundled unchanged as `../pr36845.diff`." Also update `lanes/sglang-tp2/README.md:89` ("Reference kernel: ..." sentence) accordingly. If the file is kept instead: add a header `# SPDX-License-Identifier: AGPL-3.0-or-later` / `# Copyright (C) 2026 MiaAI Lab. Extracted 2026-08-28 from MiaAI-Lab/Qwen3.8-Flash-Next-Dual-DGX-Sparks start.sh; a port of sgl-project/sglang PR #36845 (Apache-2.0, BBuf) with MiaAI's fp8 K/V extension. Reference only, not used by this lane.`, put the AGPL-3.0 text in `community-fix/LICENSE`, and add to the root README: "`lanes/sglang-tp2/community-fix/` is AGPL-3.0-or-later (MiaAI-Lab); everything else is Apache-2.0." And change lane README:22 "with no borrowed code" to "with no borrowed code in the served stack (the untested reference kernel under `community-fix/` is MiaAI-Lab's, see its README)".

O. `lanes/sglang-tp2/README.md:89` "Credit: MiaAI-Lab + the SGLang maintainers (#36806 / #36845)." -> "Credit: MiaAI-Lab for the port; BBuf (sgl-project/sglang) for #36806 and #36845." Line 102 "adopts the same upstream kernel (#36845)" -> "adopts the same upstream kernel (#36845, BBuf, merged 2026-08-30)". Line 201 "[#36845] merging +" -> "[#36845] merged 2026-08-30;". `Dockerfile.qwen38fn-sm121-triton-varlen:11` "(sglang PR #36845, open," -> "(sglang PR #36845 by BBuf, merged 2026-08-30,".

P. `single-spark-vllm-tp1/research/gb10-prior-art-and-pitfalls-2026-09-05.md:9` replace "Landed AFTER our nightly, we do not have it:" with "Landed after our nightly, applied here as `patch/upstream-overlays/ops_ple.py`:" and drop "Mitigation until rebase: ...". Line 42: "Daniel Han (`CUTE_DSL_ARCH=sm_121a` origin)" -> "Daniel Han / Unsloth (popularized `CUTE_DSL_ARCH=sm_121a` for DGX Spark; the flag itself is CUTLASS DSL's)". Optionally add "lancelind (fork of blazux, credited by MiaAI for the FP8-KV approach)".

Q. `single-spark-vllm-tp1/research/upstream-issue-modelopt-mtp-index.md:16` append: "Independently reproduced and fixed the same day (2026-09-05) by sfxnz (`sfxnz/Qwen3.8-Flash-Next-NVFP4-vLLM-2x-DGX-Spark`, `docker/modelopt.py`, commit 13:51 UTC) and MiaAI-Lab (`Qwen3.8-Flash-Next-Dual-DGX-Sparks`, `files/patch_modelopt_fp8_block_moe.py`, commit 16:07 UTC); PixelML/seanphan PR #6 hit the same error under TP2. Related upstream work: PR #50617 (FP8_PB Linear dispatch in modelopt_mixed) and PR #54367 (FP8_PER_CHANNEL_PER_TOKEN dispatch)."

R. Commit or delete `single-spark-vllm-tp1/launch/qwen38fn-nvidia-tp2.sh` (untracked).

## Fetch log

All fetches succeeded except two that are expected: `https://raw.githubusercontent.com/vllm-project/vllm/8a728663.../tests/models/qwen4_exp/test_qsa_kv_quant.py` (404, the file is new in PR #54846, not in the nightly) and the MiaAI Dual repo commit list filtered to before 2026-08-29 (API returned 0 commits; the current history starts 2026-08-31, so the 08-28 `start.sh` the kernel was extracted from could not be re-fetched; the provenance rests on our own commit 3af415e / `community-fix/README.md:12` and on the function names being absent from sglang PR #36845). `patch-diff` URLs for #54125, #54426, #54173 return HTML because those numbers are issues, not PRs; their bodies were read through the issues API instead.
